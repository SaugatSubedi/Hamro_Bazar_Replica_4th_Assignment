<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saugat.a4thassignmenthamro_bazzarreplica">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register"></activity>
        <activity android:name=".Login" />
        <activity android:name=".agreement.AdPostingRule" />
        <activity android:name=".agreement.Terms" />
        <activity android:name=".agreement.Safety" />
        <activity android:name=".model.Agreement">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".model.SplashActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>